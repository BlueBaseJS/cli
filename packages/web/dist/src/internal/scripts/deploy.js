'use strict';

var _utils = require('../utils');

const cmd = 'now'; /**
                    * Deploys to now.
                    * @see https://zeit.co/now
                    */

(0, _utils.exec)(cmd);